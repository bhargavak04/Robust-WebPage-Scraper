services:
  - type: web
    name: robust-scraping-service
    env: docker
    plan: starter
    # The build and start commands are now defined in the Dockerfile.
    # Render will automatically use the Dockerfile to build and run the service.
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
